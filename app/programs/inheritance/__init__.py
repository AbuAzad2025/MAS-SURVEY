"""
Inheritance (Fara'id) Program Module.

Isolated program container under app/programs/inheritance/.
Each program is self-contained with its own routes, services, templates
and static assets. Registration with the Flask app is done via the
register_inheritance_routes() entry point (currently not wired until the
overall structure is finalized).
"""


def register_inheritance_routes(app):
    """Entry point for wiring this program's routes into the Flask app.

    Kept as a stub until the project structure and login gateway are
    finalized. Routes will be registered with a url_prefix here.
    """
    pass
